#2. 2. When depends on co-level (in the same directory) module, you MUST create/update the Modules Dependency UML of that directory.
#6. If import the **co-directory module**: import by **module** name 
from PythonRoot.A.AB.ABB.ABBA import abba

def abbb():
    abba()